syntax = "proto3";
package grpc;

option go_package = "github.com/SoulTch/gomars_go";

import "mars-struct.proto";

service GoMars {
    rpc Action(stream ActionRequest) returns(stream ActionResponse) {}
    rpc Notice(stream GetRequest) returns(stream NoticeResponse) {}
}
